# =============================================================================
# Terraform Outputs
# Auto-generated by CDX-E Ansible Pipeline — do not edit manually.
# =============================================================================

output "vm_ips" {
  description = "VM name to management IP. Static IPs from scenario config; empty string for DHCP-assigned VMs."
  value = {
{% for vm in _tf_vms %}
    "{{ vm.name }}" = "{{ vm.cloud_init.ip if (vm.cloud_init is defined and vm.cloud_init.ip is defined) else '' }}"
{% endfor %}
  }
}
