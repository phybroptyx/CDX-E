# =============================================================================
# Ubuntu Server 16.04 - Preseed Configuration (Packer Build)
# =============================================================================
# Legacy Debian installer preseed for unattended Ubuntu 16.04 Server install.
# (Ubuntu 16.04 uses the legacy installer, NOT autoinstall/cloud-init.)
# =============================================================================

# Locale and keyboard
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# Network
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string ubuntu1604-template
d-i netcfg/get_domain string cdx.lab
d-i netcfg/get_nameservers string
d-i netcfg/confirm_static boolean true

# Mirror
d-i mirror/country string manual
d-i mirror/http/hostname string archive.ubuntu.com
d-i mirror/http/directory string /ubuntu
d-i mirror/http/proxy string

# Clock
d-i clock-setup/utc boolean true
d-i time/zone string UTC
d-i clock-setup/ntp boolean true

# Partitioning - use entire disk with LVM
d-i partman-auto/method string lvm
d-i partman-auto-lvm/guided_size string max
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# User account
d-i passwd/root-login boolean false
d-i passwd/user-fullname string CDX Admin
d-i passwd/username string cdxadmin
d-i passwd/user-password password IyTt+,uDhtR@.303
d-i passwd/user-password-again password IyTt+,uDhtR@.303

# Package selection
tasksel tasksel/first multiselect standard, openssh-server
d-i pkgsel/include string qemu-guest-agent spice-vdagent sudo curl wget
d-i pkgsel/update-policy select none

# GRUB
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

# Post-install
d-i preseed/late_command string \
  echo 'cdxadmin ALL=(ALL) NOPASSWD:ALL' > /target/etc/sudoers.d/cdxadmin ; \
  chmod 440 /target/etc/sudoers.d/cdxadmin ; \
  in-target systemctl enable ssh ; \
  in-target systemctl enable qemu-guest-agent

# Finish
d-i finish-install/reboot_in_progress note
